name: run_Python

on:
  workflow_dispatch
  

jobs:
  PYTHON:
    runs-on: self-hosted


    steps:
      - name: python checkout
        uses: actions/checkout@v2

      - name: setup
        uses: actions/setup-python@v2

      - name: run
        run: python try.py